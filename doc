#!/bin/bash
echo "shell file running.."
pkill -f "/usr/local/bin/nodemon start DOC.js"
PORT=3001
PID=$(lsof -i:$PORT | awk 'NR==2{print $2}');
if [ ! -z "$PID" ]; then
  kill -9 $PID
  echo "$PID KILLED"
fi
nodemon start DOC.js